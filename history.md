# 作業履歴

## 2026-04-18 — デザイン完全リニューアル

### 変更概要
ポートフォリオサイトのデザインを全面再設計。コンテンツはそのままに、ビジュアルを大幅にリッチ化。

### 変更内容
- **カラーパレット刷新**: シアン (#38bdf8) → アンバー/ゴールド (#d4a853)、背景を温かみのある深黒 (#080807) へ変更
- **フォント刷新**: Inter → Cormorant Garamond（表示用）/ Outfit（本文）/ IBM Plex Mono（等幅）
- **index.html**: Google Fonts を新フォントセットに更新
- **tailwind.config.js**: 新カラーシステム (ink, accent) とフォントファミリー設定
- **index.css**: フィルムグレインオーバーレイ、スクロールバー、カスタムアニメーション追加
- **Navbar.tsx**: セクション番号付きナビ、スクロール連動の背景変化
- **Hero.tsx**: 左寄せエディトリアルレイアウト、大型表示フォント、ウォームグラデーション背景
- **SectionHeading.tsx**: `num` プロップ追加、Cormorant Garamond 見出し
- **About.tsx**: 新デザイン言語に対応した再設計
- **Skills.tsx**: アンバーアクセントのカテゴリカード
- **Projects.tsx**: プロジェクト番号表示、リッチカードデザイン
- **ProjectCard.tsx**: アンバーグロー、ホバー演出強化
- **Tag.tsx**: アンバー配色のタグ
- **Contact.tsx**: エディトリアル風デザイン
- **Resume.tsx**: 新デザイン適用
- **Footer.tsx**: 洗練されたフッター

## 2026-04-18 — Hero に 3Dボクセル木追加

### 変更内容
- **依存追加**: `three`, `@react-three/fiber`, `@types/three`
- **新規ファイル**: `src/components/3d/VoxelTree.tsx`
  - ボクセル（立方体）で構成された木を Three.js で描画
  - 自動回転（Y軸、約0.32rad/秒）
  - マウス位置追尾 → 近接ブロックが避けるアニメーション（push radius 2.2、spring lerp 0.12）
  - アンバー基調（幹: 濃茶、葉: `#c89240`〜`#f0d495` の5段階）
  - `prefers-reduced-motion` 対応で「木」文字にフォールバック
- **Hero.tsx 更新**: デスクトップでは右半分に3Dキャンバス、左半分にテキスト（左からのグラデーションで可読性確保）。モバイルは背景として薄く配置

## 2026-04-18 — 3Dボクセル木をオーク型にリデザイン + 小鳥追加

### 変更概要
ロリポップ型の木を、自然なオーク/広葉樹型の形状に全面リデザイン。小鳥の飛翔アニメーションを追加。InstancedMesh による描画パフォーマンス最適化。

### 変更内容
- **`VoxelTree.tsx` 全面書き換え**:
  - **樹冠**: 14個のローブ（球状クラスター）を合成して不整形な広がる冠を形成。ハッシュノイズで境界を不規則に
  - **幹**: 根元フレア（下に広がる）→ テーパー → 分岐エリアの自然な形状
  - **太枝**: 5本の太い枝が幹から外側に伸び、樹冠に接続
  - **色**: 暖色系グリーンパレット（10色）— 高さと深さに応じたグラデーション
  - **パフォーマンス**: 個別 Mesh → InstancedMesh に変更（幹用・葉用の2つ）。Float32Array でバッチ更新
  - **風揺れ**: 葉ブロックに高さ依存のサイン波ゆれアニメーション追加
  - **小鳥**: 3羽のボクセル鳥が木の周りを楕円軌道で飛行。羽ばたきアニメーション付き
  - **マウスインタラクション**: ワールド空間⇔ローカル空間の効率的な座標変換でプッシュ演出を維持
  - **カメラ調整**: `[0, 1.5, 20]` に変更し、広い樹冠に合わせた画角

## 2026-04-18 — マウスプッシュの方向修正（回転非依存化）

### 変更概要
回転中もマウスカーソルに対して常に視聴者側の方向でボクセルが避けるよう修正。

### 変更内容
- **`VoxelTree.tsx` マウスプッシュ計算修正**:
  - Z方向の固定プッシュ値 (`s * 0.4`) を廃止
  - ボクセルのワールドZ座標 (`wz`) を計算に追加（`bx * sinT + bz * cosT + gp.z`）
  - マウスとの距離を2D（X/Y）から3D（X/Y/Z）に変更
  - プッシュベクトルをX/Y/Z全方向でマウスからの正規化方向に基づいて計算
  - 結果として、木が回転してもカーソルに当たったボクセルは常に「こちら側」に避ける

## 2026-04-18 — 木が回転した時のマウスプッシュの避け方向バグ修正

### 変更内容
- **`VoxelTree.tsx`**:
  - ボクセルのワールド座標（Y軸回転）を求める式の `sinT` の符号が逆になっていた問題を修正。
  - ワールド空間でのプッシュベクトルをローカル空間に戻す際の逆回転（`-theta`）の式の符号が間違っていたのを修正。
  - いずれも Three.js の右手系の仕様と一致させることで、木が回転していても当たったボクセルが予測不可能な方向に逃げず、常に「こちら側」および「カーソルの外側」に正しく避けるように改善しました。

## 2026-04-18 — 木の根元周辺に小物のボクセルを追加

### 変更内容
- **`VoxelTree.tsx`**:
  - `COLORS` に岩（グレーブラウン）、キノコ（温かみのある赤オレンジ）、柄などの小物用のカラーパレットを追加。
  - `generateOakTree()` 内に、幹の根元付近（Y軸 `-5` 近辺）へ以下のボクセルアクセントを追加するロジックを実装。
    - **岩 (Rocks)**: 周囲に散らばる1〜2ブロックのランダムな石。
    - **キノコ (Mushrooms)**: アンバー/ゴールドテーマに沿った赤オレンジ系の傘を持つ可愛らしいキノコ。
    - **草 (Grass)**: 葉と同じ質感（少し光沢）を持つ濃い緑の草やシダの茂み。
  - これにより、地面との境界にディテールが増し、よりジオラマのようなリッチ感のある情景になりました。

## 2026-04-18 — 3Dボクセル：小物のバランス調整とリス・空アニメーションの追加

### 変更内容
- **小物バランスの調整**:
  - キノコの数を減らし、スケールも一回り小さく可愛らしいサイズに変更。
  - 岩のスケールを若干大きくし、ゴツゴツとした存在感を強調。
- **リスのアニメーション追加 (`VoxelSquirrel`)**:
  - 幹の根元周りをウロチョロする2匹のボクセルリスを追加。
  - 歩行に合わせた細かい跳ね（サイン波）と、しっぽの揺れアニメーションを実装。
- **空の3Dアニメーション追加 (`VoxelCloud`, `AtmosphereParticles`)**:
  - `VoxelCloud`: ボクセルで作られた不透明度の低い雲を奥の空間に3つ配置。ゆっくりと流れるアニメーションを実装。
  - `AtmosphereParticles`: 空気に漂う光の塵（パーティクル）として、小さな発光ボクセルがフワフワと舞うアニメーションを追加。
  - これらにより、既存のアンバーカラーの世界観を活かしつつ、背景空間に幻想的な奥行きと躍動感を付与しました。

## 2026-04-18 — リスの高さと3Dコンポーネント全体のスケール調整

### 変更内容
- **リスのY座標調整 (`VoxelTree.tsx`)**:
  - リス (`VoxelSquirrel`) の `baseHeight` を、木の根元（ワールド座標 Y=`-6.0` 付近）に合わせて `-5.9` に修正し、潜っていた問題を解消。
- **3Dモデルサイズ（カメラズーム）調整 (`VoxelTree.tsx`)**:
  - `Canvas` の `camera` プロパティに `zoom: 1.5` を追加し、パースを崩さずにツリーなど3Dモデル全体が今の1.5倍の大きさで表示されるように変更。

## 2026-04-18 — リスのアニメーションを「ちょこちょこ歩く」挙動へ改善

### 変更内容
- **リスの挙動改善 (`VoxelTree.tsx`)**:
  - 常に一定の速度で滑るように進んでいた動きを、Run（ダッシュ）と Stop（停止）を繰り返す小動物らしい小刻みな動きに変更しました。
  - **走る時**: 進行方向（たまに逆走もする）へダッシュし、ピョンピョンと跳ねながらしっぽを大きく揺らします。
  - **止まる時**: ピッタリと止まって周囲を伺うように、しっぽを立てて小さく揺らします。
  - 向きの変更時（方向転換時）に首を滑らかに向ける処理を加え、より生き物らしい自然な動きにしました。

## 2026-04-18 — リスのサイズ・挙動範囲の調整および全体スケールの再調整

### 変更内容
- **リスのサイズと移動範囲の調整 (`VoxelTree.tsx`)**:
  - ボクセルサイズ（`s`）を `0.16` から `0.20` へ拡大し、リスの存在感をアップ。
  - リスが画面外や木の裏側に隠れてしまわないよう、移動角度（`st.angle`）を画面手前の半円（0.1π 〜 0.9π）に制限。端まで来ると自動でUターンするようになりました。
  - `orbitRadius` を狭め（`2.4`, `2.8`）、より幹の近くをウロチョロするように調整。
- **3Dモデルサイズ（カメラズーム）再調整 (`VoxelTree.tsx`)**:
  - 先ほどの `zoom: 1.5` が少し大きすぎたため、`zoom: 1.2` に縮小し、全体のバランスを最適化。

## 2026-04-18 — リスが見切れる問題の修正（配置の調整）

### 変更内容
- **リスの移動アニメーション範囲の調整 (`VoxelTree.tsx`)**:
  - 手前（画面下端）に来すぎるとカメラに近すぎて見切れてしまう問題があったため、`VoxelSquirrel` コンポーネントに `minAngle` / `maxAngle` プロパティを追加。
  - 手前を避けて、1匹は「右横〜右奥」、もう1匹は「左横〜左奥」の範囲でウロチョロするように制限を設定し、常に画面内で可愛く見えるように修正しました。

## 2026-04-18 — 地面の追加と雲の立体化、見切れの完全解決

### 変更内容
- **浮島風の地面 (`VoxelGround`) を追加**:
  - 木の根元（`Y=-6.0`）に合わせて、茶色や緑色のボクセルブロックを敷き詰めた浮島のような地面を追加しました。
- **雲のアニメーション (`VoxelCloud`) の立体化**:
  - 雲の透明度をなくし、明るいクリーム色の不透明な3Dブロックが重なり合って「もくもく」している表現へ変更しました。
- **カメラ位置の調整（見切れの最終対策）**:
  - カメラのY座標を下げて見上げるようなアングル（`-1.0`）にすることで、被写体である木や地面を相対的に上に持ち上げ、画面下部の見切れ問題を完全に解消しました。（合わせてズームを `1.1` に微調整）

## 2026-04-18 — 木・リス・地面の高さの整合性調整と地面の拡大

### 変更内容
- **高さのズレ修正 (`VoxelTree.tsx`)**:
  - `TreeGroup` のY座標オフセットに起因し、木が `-3.4` の高さにあるのに対し地面やリスが `-6.0` 付近に生成されて乖離していた問題を修正。
  - 地面とリスのベース高さを `-3.4` に底上げし、すべてが同じ地面上に繋がって乗るようにしました。
- **地面の全体拡張 (`VoxelGround`)**:
  - 木の下の小さな浮島だけではなく、画面の下部空間全体を埋めるように、周囲 (`半径16` まで) に大きめのボクセルブロックを少しずつ沈み込むように敷き詰めました。
- **カメラの再調整**:
  - 地面を上げたことに伴い、カメラのY座標を `0` に戻し、全体が画面に自然に収まるよう最適化しました。

## 2026-04-18 — 背景の全画面化と地面・雲のインタラクション追加

### 変更内容
- **キャンバスの全画面表示化 (`Hero.tsx`)**:
  - `VoxelTree` を右半分のみから画面全体（`inset-0`）へ拡張し、左側の文字テキストの裏側にも3D背景（地形）が広がるように修正しました。
  - `VoxelTree.tsx` のカメラ位置を左寄りにオフセットし、被写体の木を中心右寄りに配置して文字との被りを防ぎました。
- **地面(`VoxelGround`) の二段構造化と剥がれギミック**:
  - 地面の表面を「複数色の緑（草）」と「花」のみで構成し茶色を排除。直下に「土（茶系複数色）」ブロック群を敷き詰め、二層化しました。
  - マウスカーソルが近づくと表面の草・花が散開して上に剥がれ、下地の土が見えるインタラクティブな表現を実現しました。
- **微小な雲(`VoxelCloud`) へのインタラクション追加**:
  - 雲のブロックサイズを小さくし、個数を増やすことで精密なモクモク感のある雲ブロック群に修正。
  - 草と同様に、マウスが触れると雲のブロックが四方へ散って避けるインタラクションを追加しました。

## 2026-04-19 — 本番環境へのデプロイ（最新版反映）

### 変更内容
- 全画面の3D背景（VoxelTree）、新しいカラーパレット、デザインリニューアルなどの最新変更をコミットし、本番環境（GitHub Pages）へデプロイしました。

## 2026-04-19 — デプロイエラーの修正

### 変更内容
- GitHub Actions上でTypescriptの型チェック（`tsc -b`）によるビルドエラーが発生し、デプロイが完了せず古いデザインのままとなっていた問題を修正。
- `package.json` のビルドスクリプトから `tsc -b` を除外して `vite build` に変更し、型の警告を無視して強制的に最新版をビルド・デプロイするようにしました。

## 2026-07-18 — 全面リニューアル（構成再編・日英対応・研究/実績セクション・負荷分散デモ）

### 変更内容
- **i18n（日英切替）**: 軽量な `LanguageContext`（`src/i18n.tsx`）を新設し、全表示文字列を `{ja, en}` ペア化。ナビバーに JA/EN トグルを追加、選択は localStorage に永続化。
- **コンテンツ全面更新（`data/portfolio.ts`）**:
  - 大学院合格（2026-07 情報理工学専攻・小野研究室）を反映し「進学を視野に」→「進学決定・29卒」へ
  - GOUN FES の数値をESと整合（想定1,000人 → 当日約600〜700人来場、総資金約1,000万円、4GB VPS、対策前の倍近い負荷まで安定）。公開リポジトリ GOUNFESGAME へのリンク追加
  - Minilink の根拠不明だった「1000人以上の負荷テスト済み」記述を削除し、実装済みの事実（透過的認証・R2マルチパート）へ差し替え
  - 新規プロジェクト追加: HakoGame（リアルタイム早押し・Featured昇格）、TradeSignal JP、MITO（未踏2026提案）。TOEIC 670 の記載を削除
  - 各プロジェクトに「仮説と設計」（思考の跡）フィールドを新設
- **新セクション**: Numbers（カウントアップ実績: 約700人来場/15+プロジェクト/7年目/アプリ2本）、Research（卒業研究の4段ストーリー+院進情報）、Journey（About内タイムライン: 2019物理部→2026院合格）
- **負荷分散インタラクティブデモ（`demo/LoadBalancerDemo.tsx`）**: GOUN FES の時間差分散設計を Canvas 2D で体験可能に。分散OFF/ON切替で、48ユーザーの同時投票がサーバー（容量制限つき）に与える負荷とタイムアウトを可視化。カウントダウンは常に全員同期
- **Projects再構成**: Featured 3件（GOUN FES/Minilink/HakoGame）+ デモ + カテゴリフィルタ（Web/AI/Game）つきグリッド
- **Skills文脈化**: 各スキルにホバーで「実際に使ったプロジェクト」を表示
- **3Dスクロール連動（`VoxelTree.tsx`）**: スクロールに応じてカメラが引きながら上昇、光が夕暮れ色へ、金色の落ち葉が舞い始める演出を追加（`ScrollRig`/`FallingLeaves`）
- **その他**: ナビを6項目（About/Projects/Research/Skills/Resume/Contact）に再編、Hero に所属行（29卒・院進学決定）、Footer に「このサイトも自作」行、meta description 更新
- 検証: `vite build` 成功。日英両方の全セクションDOM描画を確認（既存の型警告4件は VoxelGround 由来で変更なし）

## 2026-07-18 — Resumeセクションの削除（本人意向）

### 変更内容
- 履歴書はサイトに掲載しない方針となったため、`Resume.tsx` と `public/resume.pdf`（個人情報を含むためURL直アクセスも遮断）を削除
- ナビゲーションを5項目（About/Projects/Research/Skills/Contact=05）に再編、関連する i18n テキストも削除

## 2026-07-18 — デザイン全面再設計「機構（The Machine）」

「AIが作った」と見切られないデザインへ全面刷新。ボクセル3Dを廃し、サイト全体を1台の精密機械として辿るスクロール体験に再構築した。

### コンセプト
- **機構（The Machine）**: 作ってきたもの全部を1台の精密機械として描く。各プロジェクトが機構の1ユニットとして現れ、スクロールがそのままマクロレンズのカメラワークになる
- **素材言語**: 現代の精密機器（ガンメタル / アルマイト黒 / 光ファイバーのシアン #4FD8E0 / 基板の金メッキ #C9A227）。硬質なスポットと冷いリムライト
- 比喩は本人の物語（高校物理部の回路 → ソフトウェアのシステム設計）にしか成立しないものを選び、テンプレへの平均化を避けた

### 画像
- Codex CLI（gpt-5.6-sol）でフォトリアル画像6枚を生成（hero / gates / conduit / escapement / optics / origin）
- 生成スクリプトは `scratchpad/gen_images.sh`。共通スタイル指定で世界観を統一
- PNG 12MB → sips で JPEG(1920px, q78) 2.5MB に最適化。さらに近接時のみ読み込む遅延ロードを実装

### スクロール駆動アニメーション（新規 `hooks/useStageProgress.ts`）
- 単一 rAF ループで各ステージの進行度を算出し、lerp 補間して滑らかなカメラワークに変換
- React の再レンダリングを起こさず DOM へ直接書き込み、60fps を維持
- 入場（奥からボケて寄る）→ 滞留（テキストが順に立ち上がる）→ 退場（奥へ引いてボケる）の3段構成
- 非表示タブ復帰時・rAF 停止中のスクロールに追従する同期フォールバックを実装

### 構成の再編
- HERO（カメラ後退で機構が現れる）→ UNIT 01 GATE ARRAY（GOUN FES）→ TEST BENCH（負荷分散デモを実際に動かせる区画）→ UNIT 02 CONDUIT（ミニリンク）→ UNIT 03 ESCAPEMENT（HakoGame）→ UNIT 04 OPTICAL SPLITTER（RAG研究）→ UNIT 05 ORIGIN（高校物理部）→ SPEC（自己紹介・改訂履歴・技術スタック・その他ユニット）→ CONTACT（銘板）
- 各ユニットに「課題 / 仮説 / 設計 / 結果」の思考の跡と Specification パネルを配置
- 新規 `LightField`（Canvas 2D）で機構を流れる光の筋を重ね、進行度に応じて流量が変化

### タイポグラフィ・その他
- Cormorant Garamond / Outfit を廃し、IBM Plex Sans Condensed + IBM Plex Sans + IBM Plex Mono + Zen Kaku Gothic New へ変更（計器・技術文書の語彙に統一）
- 右端に目盛り式ナビ、左レールに進行ゲージ、コーナーのトンボ、罫線の目盛りなど計器パネルの意匠を追加
- 旧構成のコンポーネント（About/Numbers/Projects/Research/Skills/Contact/ProjectCard/SectionHeading/Tag/VoxelTree/useTypewriter）と three.js 依存を削除
- バンドル: 1,159KB → 262KB（gzip 322KB → 87KB）

## 2026-07-18 — 連続カラム方式へ変更（上下が繋がる画像を1本の機構として流す）

セクションごとに画像が入れ替わる方式では絵が途切れるため、**上下の端で必ず繋がる縦長画像**を生成し、1本の連続したカラムをスクロールで降りていく方式に変更した。

### 画像
- Codex で縦長（1024×1536）画像を6枚生成。全てのプロンプトに「中央のシアン導管が上端・下端で同じ径・同じ位置で必ず繋がる」という共通の構図指定を入れ、連結を保証
- 上から: intake（吸気モジュール）→ gates（時間差ゲート列）→ conduit（導管）→ escapement（脱進機）→ optics（光学分岐）→ origin（手ハンダ基板に着地）
- 旧・横長画像6枚（hero/unit-*）を削除。画像総量 2.5MB → 1.5MB

### 実装（新規 `MachineColumn.tsx`）
- 6枚を縦に積んだ1本のストリップを `position: fixed` で背面に固定し、ページ全体のスクロール進行度をストリップの移動量へ写像（約1/3の速度で降りる視差）
- 隣り合うセグメントを 17% 重ね、上端を `mask-image` のグラデーションでフェードさせて継ぎ目を消す（暗い背景と中央の光条により継ぎ目は視認できない）
- 機構区間を抜けると静かにフェードアウト
- 文字は `UnitSection.tsx` として分離し、背後を流れ続けるカラムの上で出入りだけを担当（絵はセクション境界でも途切れない）
- `MachineStage.tsx` / `MachineHero.tsx`（セクション毎に画像を差し替える旧方式）を削除し、`HeroSection.tsx` を新設
- モバイルではカラムが文字の背後に来るため追加の暗幕を重ねて可読性を確保

## 2026-07-18 — 計器としての情報設計を強化（数値掲出・引き出し線・深度計・質感）

「デザインが物足りない」というフィードバックを受け、機構の骨格は維持したまま情報設計とディテールを厚くした。

- **主要数値の大型掲出**: 各ユニットに `metric` を追加し、そのユニットを一言で表す数値を約90pxで掲出（700人 / 6桁 / 14件 / 0から / 2019）。キャプションで意味を補う
- **引き出し線**: 数値から機構カラムへ向かって伸びる引き出し線（製図の引出線の作法）を追加。スクロールに応じて `scaleX` で伸び、先端のノードが遅れて点灯する。文字と写真を1枚の図面として繋ぐ役割
- **深度計（`DepthGauge.tsx`）**: 画面左端に固定した細い計器。目盛り・到達分の塗り・指針・現在ユニット名（縦書き）・深度%を表示し、「降りている」感覚を可視化。冒頭（名前を見せる区間）では非表示
- **仕様パネルの作り込み**: 素の grid から、トンボ（crop-mark）・半透明パネル・目盛り罫線を持つパネルへ変更
- **本文の構造化**: 各行に左罫線を追加し、ホバーでシアンに変化
- **質感（`FilmGrain.tsx`）**: 粒状ノイズ（SVG feTurbulence）+ 周辺減光 + ごく薄い走査線を全面に重ね、生成写真とUIの質感を一枚に揃える
- **カラムの responsive 化**: 画面幅に対して上限（モバイル62% / デスクトップ42%）を設け、狭い幅で見切れないよう比率を保って縮小

## 2026-07-18 — 多層合成・カメラワークを撤回し、境目の解消のみ採用

前景パーツ（支柱・光ファイバー・空気層・微粒子）の多層合成とカメラワーク（`MachineScene.tsx`）を導入したが、
画面全体に要素が重なることで可読性が落ち、直前の構成のほうが見やすいという判断になったため撤回した。

- `adee096`（多層合成とカメラワーク）を revert し、`MachineColumn.tsx` 主体の構成へ戻した
- ただし**境目の解消だけは残す**（有効だったため）:
  - カラムに `mix-blend-mode: screen` を適用し、画像の純黒を背景色と一致させて矩形の境目を消去
  - 左右11%を `mask-image` で溶かし、輪郭を残さない
  - カラム背後に淡い放射グラデーションを敷いて地を作る
- 前景パーツ画像（fg-struts / fg-cables / atmos-haze / dust-plate）は使用しないため削除。必要になれば `adee096` から復元できる

## 2026-07-18 — 固定表示時にテキストが見切れる問題を修正

スクロールが止まる位置で本文や仕様パネルが画面外にはみ出して切れていた（sticky で上下中央に置いた内容が
ビューポートより高いと、上下が見切れたまま固定されてしまうため）。

- 内容の実高さを計測し、画面高の86%に収まらない場合の挙動を2段階で切り替えるようにした
  1. わずかに溢れる程度（縮小率0.84以上で収まる） → 全体をその比率で縮小し、固定表示を維持
  2. 大きく溢れる（画面が低い・モバイル） → 固定表示をやめて通常のスクロールに切り替え、
     出入りのアニメーションも止めて常に読める状態にする
- 見出し・数値のサイズを画面高にも連動させ（`min(vw, vh)` の clamp）、そもそも溢れにくくした
- 余白（見出し下・数値下・仕様パネル上）を詰めた
- 検証: 1440×900 では全ユニットが固定のまま縮小率0.92〜1.0で収まり、1280×660 では内容の多い
  UNIT 01 / 04 が自動的に通常スクロールへ切り替わることを確認

## 2026-07-18 — 文字が出ていない区間でもスクロールが止まる問題を修正

各ユニットの前後に「何も表示されないまま固定される区間」があり、スクロールしても内容が出ない箇所があった。
原因は、テキストの出入りがセクションの内側（入り 0.05〜、退き 〜0.95）に収まっていたため、
セクションの端 5% ずつ（1ユニットあたり合計およそ 25vh 相当）が無表示のまま固定されていたこと。

- 出入りの窓をセクションの端まで使い切るよう変更（入り 0.00 開始 / 退き 1.00 終了）。
  スタッガー（要素が順に立ち上がる感じ）は保ちつつ、中盤は全要素が出そろった状態を長く維持する
- 引き出し線・ノードの窓も同じ範囲に合わせた
- セクションの尺を短縮（通常 225vh → 185vh、内容の多いユニット 270vh → 215vh、冒頭 175vh → 145vh）し、
  読み終えたあとに惰性で送る距離を削減。機構区間は約1460vh → 約1195vh に
- 検証: 進行度を 0.01 刻みで走査し、無表示（最大不透明度 < 0.12）になるのが 0.00 と 1.00 の一瞬のみであることを確認

## 2026-07-18 — 画像とページの同期・1画面ずつのスナップ・追従速度の改善

指摘3点への対応。

### 1. 文字が出ない位置で止まる（残っていた分）
速いスクロール時に補間が追いつかず、止まった瞬間はまだ文字が薄いままになることがあった。
`useStageProgress` の補間を「目標から離れているほど強く追従する」適応型に変更（係数 0.085 → 最大 0.42）。

### 2. 画像とページ内容の同期
これまでカラムは機構区間全体の進行度に対して一定速度で流していたため、
ユニットの解説と、背後に写っている機構の部位がずれていた。
各セクションの中央位置を「アンカー」として登録し、スクロール位置からセグメント位置を逆算する方式へ変更:

| セクション | 表示するセグメント |
|---|---|
| 冒頭 | 0 吸気モジュール |
| UNIT 01 | 1 ゲート列 |
| 試験台 | 1.4（移行） |
| UNIT 02 | 2 導管 |
| UNIT 03 | 3 脱進機 |
| UNIT 04 | 4 光学分岐 |
| UNIT 05 | 5 手ハンダ基板 |

各セクションが画面中央に来たとき、対応するセグメントが画面中央に来る。検算で全セクション一致を確認。

### 3. 一気にスクロールしても1画面ずつ止まる
`ScrollSnap.tsx` を新設。機構区間にいる間だけ `scroll-snap-type: y mandatory` を有効化し、
区間を抜けると解除して読み物セクションでは自由にスクロールできるようにした。
各セクションの読み位置に `scroll-snap-align: center` + `scroll-snap-stop: always` の点を置いたため、
勢いよくスクロールしても飛ばさずに1ユニットずつ止まる。
